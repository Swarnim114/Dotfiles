#!/bin/bash

OUTPUT_DIR="$HOME/Pictures/Screenshots"
mkdir -p "$OUTPUT_DIR"

FILENAME="$OUTPUT_DIR/screenshot-full-$(date +'%Y-%m-%d_%H-%M-%S').png"

# Capture the entire screen instantly, save it, and copy to clipboard
grim - | tee "$FILENAME" | wl-copy --type image/png
