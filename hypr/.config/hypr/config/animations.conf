# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                    Animations Configuration                 ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

animations {
    enabled = yes, please :)

    # Bezier curves
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    # General animations
    animation = global, 1, 2, default
    animation = border, 1, 2, quick
    animation = windows, 1, 1, linear
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    
    # 📌 FIX: Enabled workspace animations (Fast & Smooth)
    animation = workspaces, 0, 4, easeOutQuint, slide
    animation = workspacesIn, 0, 4, easeOutQuint, slide
    animation = workspacesOut, 0, 4, easeOutQuint, slide

    # Closing animations - minimal & fast
   animation = windowsOut, 0, 0, bezier
}
